services:
  file-sharing-web:
    container_name: file-sharing-web
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5437:80"
    env_file:
      - .env.base
    volumes:
      - .:/app
